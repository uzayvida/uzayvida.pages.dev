baseURL = "https://example.com/"
# include content marked as draft
buildDrafts = false
# include content with publishdate in the future
buildFuture = false
# include content already expired
buildExpired = false
# enable this to make all relative URLs relative to content root. Note that this does not affect absolute URLs.
relativeURLs = true
disableRSS = true
enableRobotsTXT = true
editor = "nano"
languageCode = "en"
language = "en"
theme = "vncnt-hugo"
title = "Uzay Vida"

_merge = "none"

[permalinks]
  fixed = ":title/"
  posts = "posts/:slug/"

[params]
  author = "Uzay Vida"
  # email does still work but is deprecated.
  # use [params.contact.email] instead.
  email = "info@uzayvida.com.tr"
  description = "Landing Page"
  bio = "Bedirhan Özdemir  \nbedirhan@uzayvida.com.tr \n\nMelih Gökçek Bulvarı Elektrokent 2 İş Merkezi 83/27 Ostim Yenimahalle - Ankara"
  avatar = "img/uzayvida2.png"
  favicon = "img/favicon.ico"
  error404 = "There is no such page."

  [params.contact]
    #github = "https://github.com/jdoe"
    instagram = "https://instagram.com/uzaybaglanti"
    #twitter = "https://twitter.com/jdoe"
    whatsapp = "https://wa.me/905054030630"
    
    # You may use (inline) tables for more versatility
    # This allows sorting and even more icons.
    # Just make sure not to mix key-value pairs, inline tables and tables,
    # although keeping keeping this order is ok.
    # See https://toml.io/en/v1.0.0 for details
   #0 = { name = "ello", icon = "ello", style = "fab", link = "" } # empty links are relative to site root
    #[params.contact.1] # key names are sorted lexicographically
     #name = "whatsapp" # may be omitted. defaults to key name
      #icon = "whatsapp" # may be omitted. defaults to key name
      #style = "fas" # may be omitted. default = "fas"
      #link = "" 
    #[params.contact.email]
     #name = "whatsapp" # may be omitted. defaults to key name
      #icon = "whatsapp" # may be omitted. defaults to key name
      #style = "fas" # may be omitted. default = "fas"
      #link = "mailto:bedirhan@uzayvida.com.tr"